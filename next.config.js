/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'via.placeholder.com', 'local-imge.oss-cn-beijing.aliyuncs.com'],
  },
}

module.exports = nextConfig